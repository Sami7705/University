@model IEnumerable<UniversityModel.Models.CourseAssignment>

@{
    ViewData["Title"] = "Course Assignments";
}

<h1 class="text-center mt-4">@ViewData["Title"]</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New Course Assignment</a>
</p>

<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Course.Title)</th>
            <th>@Html.DisplayNameFor(model => model.Instructor.Name)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Course.Title)</td>
                <td>@Html.DisplayFor(modelItem => item.Instructor.Name)</td>
                <td>
                    <a class="btn btn-sm btn-info" asp-action="Edit" asp-route-id="@item.CourseID">Edit</a>
                    <a class="btn btn-sm btn-secondary" asp-action="Details" asp-route-id="@item.CourseID">Details</a>
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.CourseID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
