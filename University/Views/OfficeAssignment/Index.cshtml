@model IEnumerable<UniversityModel.Models.OfficeAssignment>

@{
    ViewData["Title"] = "Office Assignments";
}

<h1 class="text-center mt-4">@ViewData["Title"]</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New Office Assignment</a>
</p>

<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Instructor.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Location)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Instructor.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Location)</td>
                <td>
                    <a class="btn btn-sm btn-info" asp-action="Edit" asp-route-id="@item.InstructorID">Edit</a>
                    <a class="btn btn-sm btn-secondary" asp-action="Details" asp-route-id="@item.InstructorID">Details</a>
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.InstructorID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
