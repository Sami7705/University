@model UniversityModel.Models.OfficeAssignment

@{
    ViewData["Title"] = "Office Assignment Details";
}

<h1 class="text-center mt-4">@ViewData["Title"]</h1>

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Office Assignment Information</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">Instructor</dt>
            <dd class="col-sm-10">@Model.Instructor.Name</dd>

            <dt class="col-sm-2">Location</dt>
            <dd class="col-sm-10">@Model.Location</dd>
        </dl>
        <a asp-action="Edit" asp-route-id="@Model.InstructorID" class="btn btn-primary">Edit</a>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</div>
